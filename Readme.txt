Load the files into google colab research and choose a appropriate GPU. 



FOR FILES 
	Pruning, 
	Quant 
	Ablation


Take the trained model file "   vgg_cifar10_trained.pth  " and upload it to the current colab runtime

it shouldn't need the path if uploaded to the working folder.
( /content/ is the path that it starts you in colab) so for example   /content/vgg_cifar10_trained.pth  


Once it is loaded, hit run all.


